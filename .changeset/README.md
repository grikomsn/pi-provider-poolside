# Changesets

Add a changeset for user-visible changes:

```sh
npm run changeset
```

Select `pi-provider-poolside` and the appropriate semantic-version bump. Documentation, tests, and repository-maintenance-only changes do not require a changeset.
